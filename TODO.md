For each tool, include the download links on that tool's page. We probably don't even need a "Downloads" page to cover them all. Moreover, MapTool belongs in
front of TokenTool.

Per page:
- /tools/maptool
  - Include the MT logo somewhere
  - What is the "[framework page]"?
  - What is the "([Read more] about our community.)" link?
  - What si the "[tutorials]" link?
- /tools/tokentool
  - Make it stylistically more in line with MapTool's page.
- /tools/archived-tools/
  - Currently not directly accessible since our navbar doesn't have the drop-down functionality that enables on the original site.
  - Redundant given each tool is present on the Tools page, so I think we can remove this page.
  - The images are in a column of 33% width, and the text sits to the right at 66%. I like this, but with vanilla minima I don't seem to have any way to flow
    text like this. I'll need to implement my own flow containers or something.
  - I want the "Download" links to be styled as buttons, as in the original.
- /tools/download-rptools-products
  - I want the "Download" links to be styled as buttons, as in the original.
